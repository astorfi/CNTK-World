language: python
python:

  - "3.5"

# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install coveralls

script:

  - coverage run --omit=*.virtualenvs*,*virtualenv* codes/p01-warmup/0-welcome/welcome.py test


after_success:
  coveralls

sudo: enabled
dist: trusty
